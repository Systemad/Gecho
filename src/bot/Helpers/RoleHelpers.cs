namespace bot.Helpers;

public static class RoleHelpers
{
    public static string CreateRoleNameForGame(this string rolename)
    {
        // maybe take first letter of each word if there are more than 2-3 words
        // otherwise just take fullname?
        return "";
    }
}
